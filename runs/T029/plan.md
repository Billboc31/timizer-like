`runs/T029/plan.md` has been written with the required four-section structure. The plan identifies two files to change — `CalendarGrid.tsx` (add click cycling, locked state, total summary element) and the two test files — plus the minimal new test in `CraMonthSelector.test.tsx` for month name display.
