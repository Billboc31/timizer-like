Plan written to `runs/T025/plan.md`. It covers: installing `react-router-dom` v7, extracting the current App.tsx logic into a `CraDetailPage`, adding a new `HistoryPage` (fetches `listCras()`, manages state, delegates PDF download), a presentational `CraHistoryTable` component with conditional Download button for VALIDATED rows only, co-located CSS and Vitest tests for both. No backend changes needed — `listCras()`, `downloadCraPdf()`, and `CraSummaryDto` are already in place.
