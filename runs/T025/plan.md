Plan written to `runs/T025/plan.md`. It covers routing setup (react-router-dom install + App.jsx wiring), the new `HistoryPage.jsx` component with loading/error/empty states, action buttons (open + conditional PDF download), plain CSS styling, and Vitest tests — all reusing existing `listCras()`, `downloadCraPdf()`, and `CraSummaryDto` without touching the API layer.
