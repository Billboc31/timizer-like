Plan written to `runs/T018/plan.md`.

The plan defines three new files under `frontend/src/api/`: `ApiError.js` (the shared error class), `apiClient.js` (the fetch wrapper), and `craApi.js` (the six named exports). Two hypotheses are flagged explicitly — the PDF and history endpoint paths — since those weren't confirmed in the backend exploration.
