Plan written to `runs/T014/plan.md`. It covers 4 new files (service, controller, two test classes), one potential DTO modification (`CraSummaryDto` + `validationDate`), and reuses the already-declared `findAllByOrderByYearDescMonthDesc()` repository method with no DB migration needed.
