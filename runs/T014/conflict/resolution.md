Push succeeded (with warnings about large conflict context files, but those are non-blocking). The rebase is complete and PR #49 has been updated.

The branch is now clean on `ticket/T014-create-cra-history-api` with state `TEST_COMPLETE`. All conflicts were in `backend/target/` build artifacts — no source code conflicts existed. The implementation of the CRA history API (`GET /api/cras`) is intact and PR #49 is updated.
