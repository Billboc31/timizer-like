The plan has been written to `runs/T023/plan.md`. It covers three new files (`CraPage.tsx`, `CraPage.css`, `CraPage.test.tsx`) and three modified files (`CalendarGrid.tsx`, `CalendarGrid.css`, `App.tsx`), with an explicit assumption that `providerSignatureDate` is auto-filled with today's date and 8 test cases covering all acceptance-criteria branches.
