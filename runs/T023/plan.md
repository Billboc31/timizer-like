Plan written to `runs/T023/plan.md`. It creates one new component (`ValidateCraButton`) with six test cases and one integration point in `App.tsx` — no changes to CalendarGrid or the API client are needed since both already support the validated state.
