Plan written to `runs/T023/plan.md`. It covers four files: `App.tsx` (wire up CRA detail fetch and CalendarGrid rendering), `CalendarGrid.tsx` (add validate button, inline confirmation, validated badge, error handling), `CalendarGrid.css` (new styles), and `CalendarGrid.test.tsx` (9 new test cases). The `validateCra` function in `craClient.ts` already exists and requires no changes.
