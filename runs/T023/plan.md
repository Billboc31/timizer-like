Plan written to `runs/T023/plan.md`. It covers 3 new files (`CraPage.tsx`, `CraPage.css`, `CraPage.test.tsx`), 4 modified files (`api/cra.ts`, `App.tsx`, `CalendarGrid.tsx/.css/.test.tsx`), and 8 test cases. The key architectural choices: `providerSignatureDate` auto-filled from today's date (no date picker), inline confirmation dialog in CraPage, locked state expressed via a CSS class on CalendarGrid, and a dependency noted on `GET /api/cras/:id` being available on the backend.
