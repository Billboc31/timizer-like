The plan is written to `runs/T023/plan.md`. It targets three files — `CalendarGrid.tsx`, its test file, and `App.tsx` if needed — while leaving the already-complete `craClient.ts` and type definitions untouched. The confirmation step uses the project's existing inline pattern (no modal library), and `providerSignatureDate` defaults to today's date automatically.
