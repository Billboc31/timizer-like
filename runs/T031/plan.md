The plan has been written to `runs/T031/plan.md`. It covers a single-container Docker deployment where Spring Boot serves both the compiled React frontend and the REST API, with named volumes for H2 database and provider signature persistence, Spring Actuator for health checks, and a `docs/deployment.md` for operational instructions.
