The plan is written to `runs/T031/plan.md`. It covers ten concrete file changes: two Dockerfiles, an nginx config, a production env file, a Docker Compose file, a root `.env.example`, a `.gitignore` update, an actuator dependency in `pom.xml`, an `application.yml` update, and a `docs/deployment.md`. Acceptance criteria map directly to each requirement in the ticket.
