Plan written to `runs/T011/plan.md`. It specifies a new `CraRetrievalService` with two methods (`getById`, `getByMonthAndYear`) and two GET endpoints added to the existing `CraController` (`/api/cra/{id}` and `/api/cra/{year}/{month}`), reusing the existing repository, mapper, exception, and DTOs with no schema changes.
