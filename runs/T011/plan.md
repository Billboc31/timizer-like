`runs/T011/plan.md` now contains a valid, forward-looking plan with all four required headings. The plan targets two GET endpoints (`/api/cra/{id}` and `/api/cra/{year}/{month}`) on `CraController`, four `@WebMvcTest` test cases, and delegates 404 handling to the existing `CraApiExceptionHandler`.
