Plan written to `runs/T032/plan.md`. It covers one config change (`frontend/vite.config.ts` gets `host: '0.0.0.0'`, `port: 5173`, `strictPort: true`) and two doc updates (root and frontend READMEs explaining how to find and use the remote URL). Everything else — backend, production, firewall, HTTPS — is explicitly excluded.
