Plan written to `runs/T032/plan.md`. Three files change: `frontend/vite.config.ts` gets `host: true, port: 5173, strictPort: true`; `frontend/README.md` gets a remote-access subsection; root `README.md` gets `npm start` corrected to `npm run dev` plus a Tailscale/LAN note. `vite.config.js` is left untouched since `.ts` takes precedence.
