The plan is written to `runs/T032/plan.md`. It targets exactly three files — `frontend/vite.config.ts` (add `server` block with `host`, `port`, `strictPort`), `frontend/README.md` (remote-access docs), and root `README.md` (fix wrong command + cross-reference) — and leaves everything else untouched.
