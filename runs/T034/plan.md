`runs/T034/plan.md` has been written with the four required sections. The plan scopes the work to two new CSS files (`tokens.css` and `base.css`) plus token-variable adoption across the five existing CSS files, all within the project's existing plain-CSS + BEM convention.
