Plan written to `runs/T021/plan.md`. It covers four files — `CalendarGrid.tsx` for click handling and state, `CalendarGrid.css` for three new modifier classes, `CalendarGrid.test.tsx` for six test cases, and `App.tsx` to propagate the updated CRA upward — with the cycle logic, locked/saving/error states, and verifiable acceptance criteria.
