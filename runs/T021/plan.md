Plan written to `runs/T021/plan.md`. It covers four files — `App.tsx` for state and cycle logic, `CalendarGrid.tsx` for click handling and modifier classes, `CalendarGrid.css` for the three new CSS states, and `CalendarGrid.test.tsx` for six test cases — with explicit exclusions and verifiable acceptance criteria.
