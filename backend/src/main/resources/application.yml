spring:
  datasource:
    url: jdbc:sqlite:./var/timizer.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    properties:
      hibernate:
        globally_quoted_identifiers: true
    open-in-view: false
timizer:
  # Local Vite default (docs/local-development.md). Override in prod via
  # TIMIZER_PUBLIC_FRONTEND_BASE_URL / env-specific application-*.yml.
  public-frontend-base-url: http://localhost:5173

cra:
  defaults:
    provider:
      raison-sociale: "Provider Company"
      adresse: "Provider Address"
    client:
      name: "Lyra Network"
      address: "109 rue de l'Ancienne Poste, 69100 Villeurbanne, France"
      contact:
        name: "Client Contact"
        email: "contact@example.com"
